---
target: index.html
total_score: 34
p0_count: 0
p1_count: 1
timestamp: 2026-07-18T19-31-06Z
slug: index-html
---
# Critique index.html — re-mesure du 18/07/2026 (après étapes 5 « niveaux » et 6 « exemples »)

Method: dual-agent (A : revue design isolée · B : détecteur + preuves). Pas d'automatisation navigateur (WSL) — injection sautée.

## Design Health Score — 34/40 (Good)

| # | Heuristique | Score | Point clé |
|---|-------------|-------|-----------|
| 1 | Visibilité de l'état | 3 | Taille du croisement catégories × niveau invisible avant « Commencer » ; compteurs `#niv` globaux |
| 2 | Système/monde réel | 4 | Français concret ; seul `#niv-note` parle en codes CECRL |
| 3 | Contrôle et liberté | 4 | Verdict annulable ×3 modes, reprise de session, sortie rassurée |
| 4 | Cohérence | 3 | Garde clavier « bouton focalisé » présent en QCM, absent en Saisie/Cartes ; `.ex-toggle` sans libellé inverse |
| 5 | Prévention des erreurs | 4 | Champ vide no-op, 3 indices de sélection vide, distracteurs anti-synonymes |
| 6 | Reconnaissance | 3 | `.adv-sub` statique (valeurs mémorisées cachées, dont Longueur 20) |
| 7 | Flexibilité | 3 | P1 clavier en Saisie ; pas de raccourci Corriger/Annuler ; undo Cartes limité à 1 pas |
| 8 | Minimalisme | 3 | Accueil dense : recherche + révision + 17 chips + niveau + segments ; or majoritaire au 1er lancement |
| 9 | Récupération d'erreurs | 4 | showLoaderError diagnostiqué + remède ; ratées listées/rejouables |
| 10 | Aide/documentation | 3 | « non classé = visible partout » jamais dit ; « maîtrisée » non défini |

Détecteur : **0 finding** (code 0). Vérifs : prefers-reduced-motion global (l.37), zéro outline:none, lang="he" sur .big-he/.qc-he/.ex-he/.sr-he/.srd-he/.f-he/.m-he, cibles 44px pointer:coarse. Anti-patterns : aucun signe d'IA générique (micro-copie incarnée, charte réellement appliquée).

## Problèmes prioritaires

1. **[P1] Entrée/Espace volés aux boutons en mode Saisie** (index.html l.1720-1725) — handler global `nextAfterInput()` + `preventDefault()` sans le garde `e.target.tagName==='BUTTON'` du QCM (l.1811) : « Voir un exemple », « En fait, je ne savais pas », `.ex-speak` inatteignables au clavier. Variante Cartes (P2) : Espace sur `.ex-toggle` au verso re-retourne la carte (handler l.1794-1800). Fix : recopier le garde. → harden
2. **[P2] Filtre Niveau silencieux sur les non-classés** — `nivOk` laisse passer les mots sans `data-niveau` sans le dire ; compteurs de chips globaux, jamais croisés. Fix : phrase dans `#niv-note`. → clarify
3. **[P2] Volume de sélection invisible avant démarrage** — le croisement ne se découvre qu'au `#counter`. Fix : « Commencer — N cartes » dans `updateStart()`. → clarify
4. **[P2] Exemple déplié hors-champ possible** au verso (`.face` overflow borné, pas de scrollIntoView). Fix : `scrollIntoView({block:'nearest'})` dans `exActivate`. → harden
5. **[P3] `.adv-sub` statique** — refléter « Aléatoire · 20 cartes · Au clic ». → clarify

## Personas

- Alex : P1 → souris obligatoire pour Corriger/exemples ; pas de raccourci d'annulation ; undo 1 pas.
- Sam : P1 (Entrée sur bouton du feedback avance la carte) ; `#flip-live` n'annonce ni inflexions ni existence d'un exemple.
- Casey (Ruben iPhone) : tap raté du pli = carte retournée ; exemple ouvert sous le pli ; 1er lancement = long défilement.
- Proche débutant : rien n'invite à cocher « Facile » ; `#niv-note` jargonne CECRL ; « (infinitif) » = métalangage.

## Observations mineures

`#selall` libellé figé · `.ex-toggle` sans état « Masquer » · exemples toujours ouverts dans le tiroir recherche (à assumer) · tap rangée recherche = audio + dépliage · apostrophes mélangées · `aria-valuemax="1"` initial · QCM × Phrases = boutons hauts.

## Questions

- Deux lampes : pourquoi « Commencer » (hors SRS) plus doré que la révision quotidienne ?
- « Facile » : vrai contrat (masquer les non-classés) ou protection du carnet ?
- La recherche au sommet du configurateur : bon endroit pour le dictionnaire ?
